The star-save landing point: when a word is starred in StoryText, a WordCapsule blooms and lands in the reader's top bar — the visible end of the save gesture.
```jsx
<WordCapsule word="lantern" justSaved onTap={replayWord} />
```

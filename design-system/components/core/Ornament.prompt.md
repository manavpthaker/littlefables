One-line: the Heritage ornament library — decoration placed with intention, never semantic (always aria-hidden).
```jsx
<Ornament kind="fleuron"/>
<Ornament kind="double-rule"/>
<Ornament kind="dropcap" letter="R" size={72}/>
```
fleuron = section break between body blocks; double-rule frames chapter openers & certificates; filigree corners on ceremonial cards ONLY; sunburst for celebration; rule-and-dot fine divider; dropcap for kid chapter openers (reader only). One fleuron placed with care beats twenty scattered.

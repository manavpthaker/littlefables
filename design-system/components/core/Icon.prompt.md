Hand-drawn-style line icon (Lucide stand-in at 2px stroke, matching --line-weight); use for every glyph — never emoji or unicode.
```jsx
<Icon name="star" size={28} color="var(--marigold)" />
```
Page must load `<script src="https://unpkg.com/lucide@0.462.0"></script>`.

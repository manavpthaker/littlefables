One-line: a square tap-target button holding a single icon; label is mandatory.
```jsx
<IconButton name="pause" label="Pause" variant="primary" size="hero"/>
<IconButton name="settings" label="Settings" variant="outline"/>
```
Variants plain/outline/filled/primary. Sizes track the density tap tokens.

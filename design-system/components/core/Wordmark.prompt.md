One-line: the tree mark + "Little Fables" lockup; the mark stands alone only for favicon/app-icon uses.
```jsx
<Wordmark markSize={56}/>
<Wordmark layout="stacked" animated/>
<Wordmark layout="mark-only" drawIn/>
```
animated = idle halo breath; drawIn = splash draw-in (once per session). On outward/static surfaces keep it still. Treat like a bookplate: when the mark is on a surface, it is the anchor.

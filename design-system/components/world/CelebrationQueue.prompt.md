One-line: celebrations arrive one at a time — dots show what waits, "one more surprise".
```jsx
<CelebrationQueue items={[{title:'A sun for your sky'},{title:'A new word kept'}]} activeIndex={0} nextLabel="one more surprise"/>
```

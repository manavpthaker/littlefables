One-line: big tappable answer blocks — ink-framed paper; picked turns forest, mercy warms brass.
```jsx
<ChoiceBlocks choices={[{id:'a',label:'To keep it safe'},{id:'b',label:'To trick the crow'}]} pickedId="a"/>
```

# The public demo household. Exists so the walkthrough film, Etsy listing
# screenshots, and any "try it" link all point at a real household with a
# real book on the shelf — not at Azi's private library.
#
# Provisioned 2026-08-04 via scripts/new-household.ts. The magic URL below
# is the shareable "see the demo" link; re-running new-household.ts mints
# a fresh token and the old one stays valid until it expires (device row
# TTL is CHILD_TOKEN_TTL_DAYS days).

household:
  slug: demo
  name: "Demo Household"
  provisioned_id: 6bb6286e-98bf-4c3c-8977-5b90c39f0b50

child:
  name: "Rosa"
  band: "4-6"
  provisioned_id: 239fc0d9-b63e-4557-81e4-7c62ddac0c9d

parent:
  # Grandma buying for grandkid is the persona this demo represents —
  # matches the buyer shape we expect from the Etsy listing.
  display_name: "Grandma June"
  email: "demo@littlefables.app"
  provisioned_id: bd38296b-3e1c-421f-a4bd-aec33e296de4

device:
  label: "Demo iPad"
  provisioned_id: bee6a41d-d769-40f8-842f-a1018bb948b6
  expires_at: "2026-11-02T23:26:24.100Z"
  # The magic URL we hand out in walkthroughs and marketing.
  magic_url: "https://littlefables.app/f/C5KeWk4ej5eq3Dq_A4ck7G36y4vya-tWwbvn2CyAfVs"

books:
  - lantern-round-pond
